# wallet_bot/config/__init__.py

# Import the centralized settings object from the settings module.
# This makes it accessible directly from the config package.
#
# Instead of: from wallet_bot.config.settings import config
# You can use: from wallet_bot.config import config

from .settings import config